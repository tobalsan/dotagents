## Commit Message Guidelines

Keep it simple. Follow what's been done in the previous commits.
If changes are related to a specific agent name, use it as prefix, e.g. `claude code: add command`, `codex: update config`.
