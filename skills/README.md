# Skills repository

This folder serves as a host for all of skills, without having them being auto-loaded by agents that automatically load any skill in `~/.agents/skills`.

This enables controlling exactly which global skills are loaded.
